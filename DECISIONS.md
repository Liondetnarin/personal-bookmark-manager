# การตัดสินใจของโปรเจกต์

บันทึกเฉพาะข้อสรุปที่ผู้พัฒนายืนยันแล้ว การตัดสินใจไม่ใช่หลักฐานว่าโค้ดถูกสร้างหรือทดสอบแล้ว

## รอบที่ 1 — 2026-09-13

### วิธีเรียนรู้

ทำตัวอย่างส่วนแรกกับ AI พร้อมคำอธิบาย จากนั้นผู้พัฒนาลองทำส่วนที่คล้ายกัน และตรวจโค้ดกับ tests ร่วมกัน ใช้ Collections เป็นตัวอย่างเริ่มต้นและ Bookmarks เป็นส่วนฝึกต่อ ไม่สร้างแอปทั้งหมดให้เสร็จก่อนให้ผู้พัฒนามีส่วนร่วม

### กรอบเวลา

ใช้ 1–2 วันเป็นเป้าหมายรอบแรก ยืดเวลาได้เพื่อให้เข้าใจส่วนสำคัญ โดยคงขอบเขตฟีเจอร์เดิม ไม่เพิ่มฟีเจอร์เพื่อชดเชยการยืดเวลา

### ลบ Collection

เมื่อลบ Collection ให้เก็บ Bookmarks ภายในไว้แบบไม่จัดกลุ่ม เจ้าของและเนื้อหาของ Bookmarks ต้องคงเดิม เลือกแนวทางนี้เพราะการลบกลุ่มจัดระเบียบไม่ควรทำให้ลิงก์ที่สะสมหายไป ผู้พัฒนายืนยันตามคำแนะนำในรอบที่ 1

หลักฐานที่ต้องมีภายหลัง: ลบ Collection แล้ว Bookmarks ยังอยู่ เข้าถึงได้โดยเจ้าของ และไม่มีความสัมพันธ์ค้างไปยัง Collection ที่ลบ

### URL ซ้ำ

อนุญาตให้บันทึก URL เดิมเป็น Bookmark ใหม่ได้ แต่ละรายการแยกกันและอาจมีชื่อหรือ notes ต่างกัน รุ่นแรกจึงไม่ต้องนิยามการรวม URL ที่ต่างกันเล็กน้อยให้เป็นรายการเดียว

หลักฐานที่ต้องมีภายหลัง: สร้างสองรายการด้วย URL เดียวกันได้ และแก้ไขหรือลบรายการหนึ่งโดยไม่กระทบอีกรายการ

## รอบที่ 2 — Q5–Q8

### ชื่อ Collection

ไม่อนุญาตชื่อซ้ำภายในเจ้าของคนเดียวกัน โดยตัดช่องว่างหัวท้ายและเปรียบเทียบแบบไม่แยกตัวพิมพ์ใหญ่–เล็ก ผู้ใช้ต่างคนตั้งชื่อเหมือนกันได้ หลักการเดียวกันต้องใช้ทั้งตอนสร้างและเปลี่ยนชื่อ

หลักฐานที่ต้องมีภายหลัง: เจ้าของเดียวกันสร้างชื่อที่ต่างกันเฉพาะช่องว่างหัวท้ายหรือตัวพิมพ์ไม่ได้ แต่เจ้าของต่างกันใช้ชื่อเดียวกันได้โดยไม่เปิดเผยข้อมูลของอีกคน

### การกรอก Bookmark

ผู้ใช้กรอก URL และชื่อด้วยตนเอง ทั้งสองช่องเป็นข้อมูลบังคับ ส่วน notes ไม่บังคับ รุ่นแรกไม่ดึงชื่อหรือเนื้อหาจากเว็บไซต์อัตโนมัติ เพื่อลงเวลากับ API, auth และการทดสอบ

### การกรองรายการ

Bookmarks มีสามมุมมอง: ทั้งหมดของตนเอง, ยังไม่จัดกลุ่ม และเฉพาะ Collection ที่เลือก ส่วน Collections กรองตามชื่อได้ รายละเอียดการจับคู่ชื่อและพารามิเตอร์จะกำหนดใน API contract

### ผลงานรอบแรก

ให้แอปรันในเครื่องและตรวจสอบได้ครบ พร้อมคำแนะนำและภาพสาธิต แยกการ deploy ออนไลน์เป็นรอบถัดไป ยังไม่เลือกหรือสร้างบริการ hosting

## รอบที่ 3 — Q9–Q11

### ซ่อนการมีอยู่ของข้อมูลผู้อื่น

สำหรับผู้เรียกที่ยืนยันตัวตนแล้ว การอ้างถึงข้อมูลของผู้อื่นให้ตอบว่าไม่พบข้อมูลเช่นเดียวกับ ID ที่ไม่มีจริง ใช้กับการอ่าน แก้ไข ลบ และรายการ Bookmarks ภายใน Collection ไม่ตอบข้อความที่ยืนยันว่าข้อมูลนั้นมีเจ้าของคนอื่น

หลักฐานที่ต้องมีภายหลัง: เปรียบเทียบสถานะและเนื้อหา error ระหว่าง ID ของผู้อื่นกับ ID ที่ไม่มีอยู่ ตรวจว่าคำขอที่ถูกปฏิเสธไม่เปลี่ยนข้อมูลของเจ้าของ

### PUT และ PATCH

PUT แทนที่ข้อมูลที่แก้ไขได้: ต้องส่งช่องบังคับครบ ช่อง notes และ collectionId ที่ไม่ส่งจะถูกล้าง PATCH แก้เฉพาะช่องที่ส่งมา ช่องที่ไม่ส่งคงเดิม และใช้ null เมื่อต้องการล้าง notes หรือออกจาก Collection ทั้งสองวิธีไม่เปลี่ยนเจ้าของหรือ ID

หลักฐานที่ต้องมีภายหลัง: ตรวจความต่างของการละช่องกับการส่ง null และตรวจว่า PUT ที่ขาดช่องบังคับถูกปฏิเสธโดยไม่เปลี่ยนข้อมูล

### ลบ Bookmark

ลบ Bookmark ถาวรหลังผู้ใช้ยืนยันในหน้าจอ ไม่มีถังขยะหรือการกู้คืนในรุ่นแรก การยืนยันในหน้าจอไม่แทนการตรวจเจ้าของใน API การลบ Collection ยังคงเก็บ Bookmarks ไว้ตามข้อสรุปรอบแรก

หลักฐานที่ต้องมีภายหลัง: ยกเลิกในหน้าจอแล้วไม่ลบ ยืนยันแล้วลบเฉพาะรายการเป้าหมาย และผู้ใช้อื่นลบรายการนั้นไม่ได้

## รอบที่ 4 — Q12–Q14

### ฐานข้อมูล

ใช้ SQLite ผ่าน Prisma สำหรับรอบแรกที่รันในเครื่อง เพื่อเรียนรู้ schema, relations, migrations และการทดสอบโดยไม่ต้องตั้ง database server การเปลี่ยนฐานข้อมูลในอนาคตต้องตรวจ schema, migrations และพฤติกรรมอีกครั้ง ดูเหตุผลใน docs/adr/0001-local-sqlite.md

### Bearer credential

API รับ Access Token ที่ออกสำหรับ API ของโปรเจกต์ ไม่ใช้ ID Token อนุญาตการเข้าถึง API ก่อน implementation ต้องตรวจ discovery/JWKS ของ Auth0 tenant ของผู้พัฒนาและบันทึก issuer, audience และ signing algorithm ที่ตรวจพบจริง ยังไม่มีการตรวจ tenant จริงในขั้นนี้

### แนวทางทดสอบ

Automated tests ใช้กุญแจและ token ทดสอบผ่านตัวตรวจ token จริงของแอป พร้อมฐานข้อมูลทดสอบแยกและผู้ใช้สอง identities ไม่ข้าม auth guard หรือปลอมผลยืนยันตัวตนแทนการตรวจ token ส่วนการล็อกอิน Auth0 จริงตั้งแต่ UI ถึง API ตรวจแยกและรายงานผลตามจริง

หลักฐานที่ต้องมีภายหลัง: token ถูกต้องผ่าน แต่ signature ผิด, issuer ผิด, audience ผิด และ token หมดอายุถูกปฏิเสธ พร้อมตรวจ ownership ของผู้ใช้สองคน การผ่าน automated tests ไม่เท่ากับตรวจ Auth0 จริงผ่านแล้ว

## สถานะ

Q15: ผู้พัฒนายืนยันแผนและรายละเอียด API contract แล้ว เริ่มเฉพาะขั้นแรกคือ scaffold, คำสั่งตรวจ, agent rules และเอกสารเรียนรู้ ไม่ข้ามไปทำแบบฝึก Bookmarks ให้เสร็จแทนผู้พัฒนา
