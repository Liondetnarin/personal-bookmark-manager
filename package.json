{
  "name": "personal-bookmark-manager",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "engines": {
    "node": ">=24.15.0 <25",
    "npm": ">=11"
  },
  "scripts": {
    "dev": "concurrently --kill-others --names api,web \"npm run dev --workspace backend\" \"npm run dev --workspace frontend\"",
    "typecheck": "npm run typecheck --workspaces",
    "build": "npm run build --workspaces",
    "check": "npm run typecheck && npm run build",
    "smoke": "node scripts/smoke.mjs",
    "auth:inspect": "node scripts/inspect-auth0.mjs",
    "test": "npm run test --workspace backend",
    "db:migrate": "npm run db:migrate --workspace backend",
    "test:e2e": "npm run build --workspace backend && node scripts/browser-tests.mjs test",
    "browser:install": "node scripts/browser-tests.mjs install chromium",
    "test:privacy": "npm run build --workspace backend && node --test backend/test/bookmarks.test.mjs"
  },
  "overrides": {
    "@prisma/config": {
      "deepmerge-ts": "8.0.0"
    },
    "prisma": {
      "mysql2": "3.24.4"
    }
  },
  "devDependencies": {
    "@playwright/test": "1.63.0",
    "concurrently": "10.0.5",
    "typescript": "5.9.3"
  }
}
